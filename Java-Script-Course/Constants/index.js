// const = a variable that can't be changed. it adds some security to code.
//formula => C = 2 * pi * r

/*let pi = 3.14;
let radius;
let circumference;

radius = window.prompt("Enter the radius of circle");
radius = Number(radius);

//pi = 4.2;

circumference = 2 * pi * radius;

console.log("The circumference is:", circumference);*/

const PI = 3.14;
let radius;
let circumference;

radius = window.prompt("Enter the radius of circle");
radius = Number(radius);

//PI = 4.2;

circumference = 2 * PI * radius;

console.log("The circumference is:", circumference)


//=> if you assign the value to const variable we can't change the value.