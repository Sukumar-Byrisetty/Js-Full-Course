let fruits = ["banana", "apple", "mango", "orange", "pineapple"];

//fruits = fruits.sort(); // => sort: it will arrange alphabetical order
fruits = fruits.sort().reverse(); // => it will arrange reverse.

for(let fruit of fruits) {
    console.log(fruit);
}