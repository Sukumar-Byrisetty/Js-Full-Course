// array = think of its a variable that can store multiple values.

let fruits = ["apple", "orange", "banana"];

//fruits[2] = "mango"; //=> we can update an element

// fruits.push("lemon");    // => we can add an element.
// fruits.pop();            // => it removes last element.
// fruits.unshift("mango"); // it can add element to beginning.
// fruits.shift();          // => it can remove element from beggining.

let length = fruits.length;
let index = fruits.indexOf("banana");

console.log(index);


console.log(fruits[1]);
// console.log(fruits);